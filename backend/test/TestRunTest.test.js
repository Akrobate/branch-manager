'use strict';

/**
 * Dummy test just to test if travis integration works correctly
 */

describe('TestRunTest', () => {
    it('Step should pass always properly', (done) => {
        done();
    });
});
