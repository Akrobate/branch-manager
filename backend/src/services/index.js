const {
    GitService,
} = require('./GitService');

const {
    CredentialService,
} = require('./CredentialService');

const {
    ProjectService,
} = require('./ProjectService');

const {
    ProcessService,
} = require('./ProcessService');

module.exports = {
    CredentialService,
    GitService,
    ProjectService,
    ProcessService,
};
