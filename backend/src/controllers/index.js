'use strict';

const {
    CredentialController,
} = require('./CredentialController');

const {
    ProjectController,
} = require('./ProjectController');

const {
    ProcessController,
} = require('./ProcessController');

module.exports = {
    CredentialController,
    ProjectController,
    ProcessController,
};
